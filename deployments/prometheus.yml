global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/prometheus-rules.yml

scrape_configs:
  - job_name: distributed-counter
    static_configs:
      - targets:
          - node-a:8080
          - node-b:8081
          - node-c:8082
